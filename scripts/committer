#!/usr/bin/env bash
set -euo pipefail

if [ "$#" -lt 2 ]; then
  echo 'usage: scripts/committer "commit subject" <path> [path...]' >&2
  exit 2
fi

subject="$1"
shift

git add -- "$@"
git commit -m "$subject"
