#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
export PYTHONPATH="$repo_root/src:${PYTHONPATH:-}"

exec micromamba run -n twopy python -c 'import sys; sys.argv[0] = "scripts/launch_napari"; from twopy.napari import main; main()' "$@"
